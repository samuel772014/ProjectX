@model IEnumerable<MVC_Project.Models.OfficialPhoto>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PhotoPath)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Activity)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PhotoPath)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Activity.ActivityID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.OfficialPhotoID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OfficialPhotoID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OfficialPhotoID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
